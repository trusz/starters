build:
	docker-compose build ui-lib
	docker-compose down

run:
	docker-compose run --service-ports --use-aliases --rm ui-lib-run
	docker-compose down

dev: 
	docker-compose run --service-ports --use-aliases --rm ui-lib-dev
	docker-compose down	